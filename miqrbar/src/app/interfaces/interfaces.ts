import { URL, Url } from 'url';

export interface Bar{
    nombre:string,
    carta:string,
    foto:string,
    color:string,
    descripcion:string,
    valoracion:number,
    numeroBar:number,
    url:string,
    pulsado:boolean
}